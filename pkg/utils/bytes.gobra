package utils

// wrapper predicate to "hide" the quantified permissions
pred BytesMem(s []byte) {
  acc(s) && s != nil
}

ghost
requires acc(BytesMem(s1), _) && acc(BytesMem(s2), _)
decreases
pure func BytesEqual(s1, s2 []byte) bool {
	return GetBytesContent(s1) == GetBytesContent(s2)
}
